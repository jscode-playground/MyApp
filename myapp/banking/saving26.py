import numpy as np
import datetime as dt
WEEKS = np.array(range(1, 27))  # 1~26 배열
#print(WEEKS)
class Saving_Model:
    num=int
    deposit_amount = int
    interest_rate = float(3)
    date = dt.date.today()

    def __init__(self):
        return 0



